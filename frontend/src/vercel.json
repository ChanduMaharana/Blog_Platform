{
  "version": 2,
  "builds": [
    {
      "src": "package.json",
      "use": "@vercel/static-build",
      "config": {
        "distDir": "dist/blog-platform"
      }
    }
  ],
  "routes": [
    {
      "src": "/post/(.*)",
      "dest": "/index.html"
    }
  ]
}
